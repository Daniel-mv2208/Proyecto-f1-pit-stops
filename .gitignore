# Virtual environments
.venv/
venv/
env/
ENV/

# Python bytecode and cache
__pycache__/
*.py[cod]
*$py.class

# Environment variables and secrets
.env
.secrets

# Large data files (Optional, but recommended)
# Uncomment the lines below to avoid committing large CSV files to GitHub.
# Collaborators can download the source archive separately and extract it to 'data/raw/'.
data/raw/*.csv
data/processed/*.csv

# Editor configuration files (VS Code, PyCharm, Jupyter)
.vscode/
.idea
.ipynb_checkpoints/